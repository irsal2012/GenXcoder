{
  "name": "mcp-gateway",
  "version": "1.0.0",
  "description": "MCP Gateway for Agent Service - provides MCP protocol access to multi-agent capabilities",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc && node -e \"require('fs').chmodSync('build/index.js', '755')\"",
    "start": "node build/index.js",
    "dev": "tsc && node build/index.js",
    "clean": "rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "agents",
    "ai",
    "gateway"
  ],
  "author": "Multi-Agent Framework",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "axios": "^1.6.2",
    "typescript": "^5.3.3",
    "@types/node": "^20.10.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
